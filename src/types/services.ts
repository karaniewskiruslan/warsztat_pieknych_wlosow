export type Services = {
  id: number; 
  image: string;
  name: string;
  options: string[];
  category: string;
  cost: number | number[];
}

export type ServicesAPI = {
  image: string;
  name: string;
  options: string[];
  category: string;
  cost: number | number[];
}